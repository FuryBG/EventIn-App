import React from 'react'
import { PlayButtonStyled } from './PlayButton.styled'


export default function PlayButton({ onClick }) {
  return (
    <PlayButtonStyled onClick={onClick}>

    </PlayButtonStyled>
  )
}
